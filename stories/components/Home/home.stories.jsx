import React from "react";
import FirstButton from "../../../components/FirstButton";

export default {
    title: "FirstButton",
    component: FirstButton,
};

export const HomePage = () => <FirstButton />;